# ===================================================
# GREEN AHOM FEDERATION — Frontend Environment
# ===================================================
# Copy this file to `.env` and fill in your credentials.
# ===================================================

# Razorpay PUBLIC key (safe for frontend — this is NOT the secret)
VITE_RAZORPAY_KEY_ID=rzp_test_XXXXXXXXXX

# Backend server URL (the Express server in /server)
VITE_PAYMENT_API_URL=http://localhost:5000
